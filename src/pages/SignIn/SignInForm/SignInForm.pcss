.form-item-label {
  @apply flex py-4 justify-between;
}

.signin-input {
  @apply border-zinc-500 border-2;
}

.form-submit-wrapper {
  @apply flex justify-center;

  & .submit-button {
    @apply bg-zinc-500 px-4 py-2 rounded cursor-pointer;
  }
}
